# Dev dependencies for tools that use plain pip.
# The authoritative source is pyproject.toml [tool.uv.dev-dependencies].
#
# To regenerate:
#   uv export --extra dev --no-hashes -o requirements-dev.txt
#
# To install with uv (preferred):
#   uv sync --extra dev

-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
