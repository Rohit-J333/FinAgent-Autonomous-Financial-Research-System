# This file exists for tools (Docker, CI) that use plain pip.
# The authoritative source is pyproject.toml — managed by uv.
#
# To regenerate after changing pyproject.toml:
#   uv export --no-dev --no-hashes -o requirements.txt
#
# To install with uv (preferred):
#   uv sync
#
# To install with pip (fallback):
#   pip install -r requirements.txt

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
langgraph>=0.1.0
langchain-google-genai>=1.0.0
langchain-core>=0.2.0
google-generativeai>=0.7.0
httpx>=0.27.0
aiohttp>=3.9.0
transformers>=4.40.0
torch>=2.2.0
qdrant-client>=1.9.0
sentence-transformers>=3.0.0
python-dotenv>=1.0.0
websockets>=12.0
pydantic>=2.7.0
edgartools>=5.23
yfinance>=0.2.36
redis>=5.0.0
upstash-redis>=1.0.0
langchain-mcp-adapters>=0.1.8
mcp>=1.0.0
langfuse>=2.0.0
